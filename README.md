# ULTRON shell

Ultron is a custom zsh shell with pluggins.

### Installation

In terminal:

`git clone https://github.com/wyshiko/ultron-shell`

`cd ultron-shell`

`chmod +x install.sh`

`./install.sh`
